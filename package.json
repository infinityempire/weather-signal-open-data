{
  "name": "dependency-stability-lens",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Source-linked AI and cloud dependency stability tracker with Upstash caching and Netlify static deployment.",
  "scripts": {
    "collect": "node src/collect.js",
    "build": "node src/build.js",
    "test": "node --test",
    "lint": "node --check src/*.js && node --check test/*.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "dotenv": "^16.6.1" }
}
