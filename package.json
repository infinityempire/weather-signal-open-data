{
  "name": "weather-signal-open-data",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NWS open-data weather signal dashboard with Upstash caching and GitHub Pages publishing.",
  "scripts": {
    "collect": "node src/collect.js",
    "build": "node src/build.js",
    "test": "node --test",
    "lint": "node --check src/*.js && node --check test/*.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "dotenv": "^16.6.1" }
}
